import nodemailer from "nodemailer"; interface InvitationEmailData { to: string; name: string; eventName: string; invitationLink: string; }; export const sendInvitationEmail = async ({ to, name, eventName, invitationLink, }: InvitationEmailData) => { try { const emailUser = process.env.EMAIL_USER; const emailPassword = process.env.EMAIL_APP_PASSWORD?.replace(/\s/g, ""); if (!emailUser || !emailPassword) { throw new Error( "EMAIL_USER or EMAIL_APP_PASSWORD is missing from .env" ); } console.log("Gmail user configured:", !!emailUser); console.log( "Gmail app password configured:", !!emailPassword ); const transporter = nodemailer.createTransport({ host: "smtp.gmail.com", port: 465, secure: true, auth: { user: emailUser, pass: emailPassword, }, }); await transporter.verify(); console.log("Gmail SMTP connection verified ✅"); const mailOptions = { from: `EventFlow <${emailUser}>`, to, subject: `You're invited to ${eventName}`, html: ` <div style=" font-family: Arial, sans-serif; max-width: 600px; margin: auto; padding: 30px; border: 1px solid #e5e7eb; border-radius: 10px; "> <h1>You're Invited!</h1> <p>Hello ${name},</p> <p> You have been invited to join <strong>${eventName}</strong> on EventFlow. </p> <p> Click the button below to accept your invitation and create your EventFlow account. </p> <div style="margin: 30px 0;"> <a href="${invitationLink}" style=" display: inline-block; padding: 12px 24px; background: #2563eb; color: white; text-decoration: none; border-radius: 6px; " > Accept Invitation </a> </div> <p> If the button doesn't work, copy and paste this link: </p> <p>${invitationLink}</p> <p>This invitation will expire in 7 days.</p> <p> Regards,<br /> <strong>EventFlow Team</strong> </p> </div> `, }; const info = await transporter.sendMail(mailOptions); console.log( "Invitation email sent successfully:", info.messageId ); return info; } catch (error) { console.error( "Send invitation email error:", error ); throw new Error( "Failed to send invitation email" ); } };